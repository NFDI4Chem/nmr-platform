APP_NAME="NMR Platform"
APP_ENV=development
APP_KEY=base64:sdkyPsaVUj7+u0PSq9qbO0v9H3af6VYf/veo7rgS/78
APP_DEBUG=true
APP_URL=https://platform.fsu.nmrxiv.org
DOMAIN=https://platform.fsu.nmrxiv.org
ASSET_URL=https://platform.fsu.nmrxiv.org

# Locale
APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Cookie Consent
COOKIE_CONSENT_ENABLED=true

# Laravel Octane
OCTANE_SERVER=frankenphp
OCTANE_HTTPS=true
SERVER_NAME=:80

# Database
DB_CONNECTION=pgsql
DB_HOST=pgsql
DB_PORT=5432
DB_DATABASE=nmr_prod
DB_USERNAME=nmr_user
DB_PASSWORD=password

# Redis
REDIS_HOST=redis
REDIS_PASSWORD=secret
REDIS_PORT=6379
REDIS_CLIENT=phpredis

# Cache & Sessions
CACHE_STORE=redis
SESSION_DRIVER=redis
SESSION_LIFETIME=120
SESSION_ENCRYPT=true
SESSION_SECURE_COOKIE=true

# Queue
QUEUE_CONNECTION=redis
HORIZON_REDIS_CONNECTION=horizon

# Broadcasting
BROADCAST_CONNECTION=redis

# File Storage
FILESYSTEM_DISK=s3
FILAMENT_FILESYSTEM_DISK=s3
MEDIA_DISK=s3

# AWS S3 / MinIO Configuration
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=nmr-platform
AWS_USE_PATH_STYLE_ENDPOINT=true
# AWS_ENDPOINT=http://localhost:9000
# AWS_URL=http://localhost:9000

AWS_ENDPOINT=http://host.docker.internal:9000
AWS_URL=http://host.docker.internal:9000


# MinIO Credentials
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123
MINIO_SERVER_URL=http://localhost:9000
MINIO_BROWSER_REDIRECT_URL=https://platform.fsu.nmrxiv.org
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_DEFAULT_BUCKETS=nmr-platform
MINIO_CONSOLE_ADDRESS=:9001

# Mail Configuration
MAIL_MAILER=smtp
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="info.nmr-platform@uni-jena.de"
MAIL_FROM_NAME="${APP_NAME}"

# Search
MEILISEARCH_HOST=http://meilisearch:7700
MEILISEARCH_KEY=your_secure_meilisearch_key

# Logging
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_LEVEL=error
LOG_DAILY_DAYS=14

# Security
BCRYPT_ROUNDS=12

# Performance
PHP_CLI_SERVER_WORKERS=4

# Docker Configuration
WWWGROUP=1000
WWWUSER=1000
