{
    frankenphp
    order php_server before file_server
}

:80 {
    root * /var/www/html/public
    
    # Health check endpoint
    handle /health {
        respond "OK" 200
    }
    
    # Laravel PHP handler
    php_server
} 