# MinIO Standalone Configuration

# MinIO Credentials
MINIO_ROOT_USER=REPLACE_WITH_MINIO_USER
MINIO_ROOT_PASSWORD=REPLACE_WITH_SECURE_PASSWORD

# MinIO Server Configuration
MINIO_SERVER_URL=http://localhost:9000
MINIO_BROWSER_REDIRECT_URL=http://localhost:9001

# Bucket Configuration
MINIO_DEFAULT_BUCKETS=nmr-platform-prod:public

# Console Access
MINIO_CONSOLE_ADDRESS=:9001

# Optional: SSL Configuration
# MINIO_CERT_FILE=/path/to/cert.pem
# MINIO_KEY_FILE=/path/to/key.pem 